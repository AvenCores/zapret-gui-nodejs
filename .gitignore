node_modules/
out/
dist/
.data/
*.log
.DS_Store
Thumbs.db
# Local user data clone (dev only)
# Keep bundled binaries in git (required for offline installer),
# but ignore upstream checkout leftovers if ever re-extracted.
zapret-discord-youtube-main/
